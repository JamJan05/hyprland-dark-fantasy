// -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- --
//  ON/OFF TOGGLE.
//
//  Since stage 7 of the redesign this is just a name compatible with old call sites
//  (Toggle in the Network and Bluetooth sections, in ToggleRow). The look comes from
//  PrzelacznikTekstowy.qml: "ON / OFF" in small caps, like options in the
//  Dark Souls menu.
//
//  A phone-style switch knob used to be here - a gold track with a light handle,
//  carried over from "Nie przeszkadzać" in swaync/style.css. In an interface
//  without rounded corners and with text options it looked like an element from
//  another application.
// -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- --

import QtQuick
import qs

PrzelacznikTekstowy {
}
